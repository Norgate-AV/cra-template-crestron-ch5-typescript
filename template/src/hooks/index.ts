/**
 * Place any custom hooks in this directory.
 * Ideally use a subdirectory for each.
 *
 * For example:
 *
 * hooks/
 * |-> useSomeCustomHook/
 * |   |-> index.ts
 * |-> index.ts
 *
 * Then export the hook from this file:
 * export { useSomeCustomHook } from "./useSomeCustomHook";
 * or
 * export * from "./useSomeCustomHook";
 *
 * Then import the hook from the hooks directory:
 * import { useSomeCustomHook } from "../hooks";
 */

export {};

/**
 * Place any containers in this directory.
 * Ideally use a subdirectory for each.
 *
 * For example:
 *
 * containers/
 * |-> index.ts
 * |-> SomeContainer/
 *     |-> index.tsx
 *
 * Then export the container from this file:
 * export { SomeContainer } from "./SomeContainer";
 * or
 * export * from "./SomeContainer";
 *
 * Then import the container from the containers directory:
 * import { SomeContainer } from "../containers";
 */

export {};
